# Odin Recipes
This simple recipe project helped me to understand lot of HTML basics. 
Such as the importance of `index.html`, using correct folder structure,
linking different pages using `a` tag, how to navigate `paths`, and the
use of semantic HTML.

Furthermore, this project also made me familiar with some of the common
and most used git commands like `add`, `commit`, `log` and `push`.